meta {
  name: Stack Actions
  type: http
  seq: 12
}

post {
  url: {{baseUrl}}/api/v1/environments/1/stacks/actions
  body: json
  auth: none
}

headers {
  content-type: application/json
}

body:json {
  {
    "action": "restart",
    "ids": ["bruno-demo"]
  }
}

assert {
  res.status: eq 200
}
