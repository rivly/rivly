meta {
  name: Container Logs
  type: http
  seq: 14
}

get {
  url: {{baseUrl}}/api/v1/environments/1/containers/REPLACE_WITH_CONTAINER_ID/logs?tail=100
  body: none
  auth: none
}

params:query {
  tail: 100
}

docs {
  Server-Sent Events stream (text/event-stream). The response streams until the
  connection is closed, so this request will not "complete" on its own.
}
