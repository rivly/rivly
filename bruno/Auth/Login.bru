meta {
  name: Login
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/v1/login
  body: json
  auth: none
}

headers {
  content-type: application/json
}

body:json {
  {
    "email": "{{email}}",
    "password": "{{password}}"
  }
}
